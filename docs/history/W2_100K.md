# W3-100k: split-KV attention + 100k bootstrap — 29.63 tok/s @100k, Tier-1 EXACT

## Result (final run: ~/w100k_goal4.log, SKV=1 SKV_S=128 SKV_CTXK=100352, 2026-09-14)
| metric | value |
|---|---|
| context | 97,810-token prompt (canonical P=97810), 60 tokens |
| Tier-1 exactness (spec K=2 == engine T=1 greedy) | 60/60 bit-exact, deterministic |
| engine T=1 vs canonical stock baseline (shifted 1) | 59/59 |
| tok/s (best of 2 timed reps) | 29.63 (93.93 ms/cyc) |
| T=1 engine reference at 100k | 16.37 tok/s (61.07 ms/tok) |
| alpha(pos) | 0.892 |
| tokens/cycle | 2.78 |
| cycle breakdown | draft 5.30 + probe 87.45 + accept 1.14 ms |
| probe kernel count | 484 (452 + split-KV trio x16) |

GATE >= 40 tok/s: NOT MET (29.63). Attribution below.

## What was built (engine0/, committed)
- skv.cu + build_skv.py per-kernel cubins: split-KV trio.
  spk_pre{1,3}: aattn3/a_attn phase-1 verbatim (q-norm + partial RoPE -> qw
  fp32 workspace with 0.0625 folded; k-norm + RoPE + fp16 KV append).
  spk_a{1,3}: grid (4 kv-groups x S=128 splits) x 256thr, GQA-shared (ONE KV
  read serves 6 q-heads x ROWS rows), 8 warps lockstep over l stride 1 with
  pragma unroll 4, warp w owns rows w/w+8/w+16, per-row online softmax in
  registers, (m,s,acc) partials to workspace, empty split writes identity.
  spk_c{1,3}: grid 24, sequential fixed-order combine (aattn3 epilogue
  structure) + sigmoid gate.
- T=3 per-row op order == T=1 (validated BITWISE). test_skv.py MODE=py
  (poison-first, pos=2000): relerr vs old aattn3 2.55e-07 (gate 1e-3), vs
  numpy-fp64 2.61e-04, appended-KV rows bit-identical.
- bootstrap: mtp_v3.py MTP_SNAP100K=1 hook (ckpt@97200 resume -> chunked
  prefill to 97810 -> ~/snap100k/ engine-layout snapshot; the mtp_v3 run also
  reproduced GREEDY 60/60 + 7.30 tok/s canonical). bootstrap_100k.py kept as
  the eager feeder (bound-var law; superseded by the mtp_v3 path).
- test_w100k.py: SHIFTED seeding (post-prefill snapshot: all P fed, cur0 =
  first predicted): engine pos_slot=P, tok_slot=cur0; engine 60 outs =
  spec_base_100k[1:]. fill_draft once (97810 positions, 375s).
- trunk_w1c.py/mtp.py: env-gated SKV=1 SKV_CTXK= SKV_S= trio swap (probe T=3,
  trunk T=1, draft T=1 + fill_draft).
- trunk.py restore(): KV pad-to-2304 FIX (stock snap is 2048 rows; CTXK-stride
  slab math on undersized buffers = OOB append writes = the device-fault class
  that fired at the 2nd spk_pre1).

## 2k with SKV (w2_skv_final.log): 40.47 tok/s (58.48 ms/cyc); the SPEC path
## reproduces the canonical stock-agreed sequence exactly + deterministic; the
## T=1-trio reference flipped a degenerate near-tie repeat region at 2k
## ([248044,198,...] vs [3204,40224,...]) — the 2.55e-07 trio-vs-aattn3
## deviation flips ties in that unstable region. At 100k (healthy region)
## spec==T1 60/60 AND stock 59/59.

## THE WALL: K1S streaming BW plateau ~160-190 GB/s (700 target)
Bench (MODE=bench, 411.9MB KV, pos=97810): v1 S=32 162.6, S=64 184.8, S=128
188.6 (production), unroll8 182.3; v2 runtime-ring 113.9 (dynamic index ->
local memory); v3 tile double-buffer 122-142. Same plateau class as the old
campaign split-KV (124-160). Probe attention = 6.59GB/190GB/s = 34.8ms of the
87.45ms probe; non-attn probe 52.7ms (weights-bound).

## Routes to 40+ (ranked)
1. Attention without per-l cross-lane reductions: the per-(row,l) shuffle tree
   is the poison — QK as a true GEMM (lanes over L, K tile via smem;
   single-array smem staging ONLY per the dext hang law; cp.async PARKED).
2. int8-per-channel KV (Tier-2, regen baseline): halves KV bytes -> ~36.5.
3. Scan grid split + GEMV occupancy: +1.5 tok/s.
4. K=3: alpha(pos)=0.892 gives depth-2 ~0.80, but probe T=4 +8ms is net
   negative until attention is fixed.

## Laws banked this session
- Build-script cubin-name clobber: the first split-KV build overwrote the W2
  k2s3.cu/.cubin (name collision in the job list) -> every later fault at
  kernel 3 was the clobbered kernel with a shifted signature. git status
  engine0/ BEFORE debugging fault classes after adding cubins.
- mtp_v3 has no module-level np: snapshot hooks must import numpy locally.
- Plain-int start_pos on the eager stock path recompiles per position (~20s/
  token at 100k shapes); bound-UOp var is mandatory.
- Killing a GPU-locked python (even compile-bound) watchdog-reboots the host;
  docker then needs the wipe runbook (rm -rf /var/lib/docker/* /var/lib/
  containerd/* inside VM + setup_nvcc_osx.sh, ~5 min).
- Engine live set ~21.6GB at 100k; replaced-handle P.up orphans (~755MB per
  full reset) OOM after ~4 resets — cap full resets, window-upload later reps.
- Engine/spec token compare: engine[0:59] vs spec_base_100k[1:60] (59/59).

## Run
cd ~/tinygrad-metal/engine0 && env SKV=1 SKV_S=128 SKV_CTXK=100352 DO_T1=0 DEV=NV
PATH/DOCKER_HOST as usual; python -u test_w100k.py (needs ~/snap100k/ +
packed/ + draft_pack/). 2k gate: SKV=1 test_w2.py.
